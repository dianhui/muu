<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:background="@drawable/bg_chapter_list_item"
    android:minHeight="50dp">
    
    <TextView
        android:id="@+id/tv_chapter_text"
        style="@style/ChapterListTextAppearence"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="15dp"
        android:gravity="left"
        android:text="@string/category_item_text" />
    
    <ImageView 
        android:id="@+id/imv_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:src="@color/light_gray"
        android:paddingLeft="5dp"
        android:contentDescription="@string/content_des_place_holder"
        />

</RelativeLayout>